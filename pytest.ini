[pytest]
testpaths = tests
addopts = -q
# Garante a raiz do projeto no sys.path para que `pytest` e `python -m pytest`
# importem config/ e scripts/ do mesmo jeito (sem isso, o `pytest` puro não
# acha o pacote `scripts`).
pythonpath = .
